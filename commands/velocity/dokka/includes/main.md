# Module voxels-core-commands-velocity

Voxels command framework implementation for Velocity platform.

### Setup with Gradle

```kotlin
dependencies {
    implementation("${dependency_notation}")
}
```